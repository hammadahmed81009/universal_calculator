"""
Data seed scripts live in this package.

Each module should be named with an ordered prefix, for example:

  001_initial_catalog.py
  010_demo_bids.py

and must define:

  SEED_NAME: str
  def run(session: Session) -> None
"""

